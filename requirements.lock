# requirements.lock — 固化依赖精确版本
# 生成日期: 2026-07-19（同步 prometheus-client）
# 用法: pip install -r requirements.lock
# 环境: Python 3.11

# ── Web 服务 ──
fastapi==0.115.12
starlette==0.45.3
uvicorn==0.34.2
websockets==13.1
python-multipart==0.0.20
sse-starlette==2.2.1
structlog==24.4.0

# ── HTTP 客户端 ──
httpx[http2]==0.28.1
h2==4.3.0

# ── 图像处理 ──
Pillow==11.2.1

# ── 数值计算 ──
numpy==1.26.4

# ── 本地 AI 推理（onnxruntime + GenAI + 向量检索）──
# 锁定精确版本：requirements.txt 只给范围，CI 解析到新版本时与 onnxruntime-genai
# 的 native lib ABI 不匹配，导致 frozen exe 在 Linux 加载 genai so 失败（Windows
# 因 DLL 搜索路径宽松不受影响）。以下版本经板端 frozen 构建实测兼容。
onnxruntime==1.28.0
onnxruntime-genai==0.15.2
onnx==1.22.0
sqlite-vec==0.1.9
tokenizers==0.23.1

# ── CLI 美化 ──
rich==13.9.4

# ── SILK 音频编码 ──
pilk==0.2.4

# ── 系统监控 ──
psutil==6.1.0

# ── 可观测性: Prometheus 指标导出 (/metrics 端点) ──
prometheus-client==0.25.0

# ── 核心 ──
python-dotenv==1.1.0
loguru==0.7.3
openai==1.82.0
aiosqlite==0.22.1
pydantic==2.13.4
jieba==0.42.1
pyyaml==6.0.2
cryptography==43.0.3
certifi==2024.12.14

# ── QQ Bot ──
qq-botpy==1.2.1

# ── 搜索 ──
duckduckgo-search==8.1.1

# ── 文档处理 ──
pdfplumber==0.11.9
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
html2text==2025.4.15

# ── 桌面模式 ──
pywebview==5.4

# ── 认知架构 ──
networkx==3.4.2

# ── 开发/测试 ──
pytest==8.3.5
pytest-asyncio==0.24.0
pytest-cov==6.1.1
pytest-timeout==2.3.1
ruff==0.11.10