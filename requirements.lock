# requirements.lock — 直接依赖精确 pin
# 生成日期: 2026-08-23（技术债 P0-1：与 .venv 实测对齐，修复 openai 跨大版本冲突）
# 用法: pip install -r requirements.lock
# 环境: Python 3.11 / 板端 Orange Pi ARM64 实测版本
# 约束: 每个包的 == 版本必须满足 requirements.txt 的范围约束（pre-push 校验）
#
# 注意（2026-08-24 如实修订）：本文件只固化「直接依赖」的精确版本，
# 不是传递闭包哈希锁（非 --no-deps + --require-hashes 的完整供应链锁定）。
# 间接依赖仍由 pip 按上界解析，构建不完全可复现；升级到哈希闭包锁为后续待办。
#
# 注意（2026-08-30 追加）：packaging 为插件容器化版本说明符解析引入的
# 直接依赖（plugins/manager.py 顶层导入），钉 26.2（板端 .venv 实测）。

# ── Web 服务 ──
fastapi==0.136.3
starlette==1.3.0
uvicorn==0.49.0
websockets==16.0
python-multipart==0.0.32
structlog==26.1.0

# ── HTTP 客户端 ──
httpx[http2]==0.28.1
h2==4.3.0

# ── 图像处理 ──
Pillow==12.2.0
qrcode==8.2

# ── 数值计算 ──
# 板端实测组合：onnxruntime 1.28.0 + numpy 2.4.6（requirements.txt 注释同源）
numpy==2.4.6

# ── CLI 交互补全 ──
prompt-toolkit==3.0.53

# ── SILK 音频编码 ──
pilk==0.2.4

# ── 系统监控 / 发行版识别 ──
psutil==7.2.2
distro==1.9.0

# ── 可观测性: Prometheus 指标导出 (/metrics 端点) ──
prometheus-client==0.25.0

# ── 核心 ──
python-dotenv==1.2.2
loguru==0.7.3
openai==2.41.0
aiosqlite==0.22.1
pydantic==2.13.4
jieba==0.42.1
rapidfuzz==3.14.5
pyyaml==6.0.3
cryptography==48.0.0
certifi==2026.5.20
packaging==26.2

# ── 本地 AI 推理（onnxruntime + GenAI + 向量检索）──
# 锁定精确版本：CI 解析到新版本时与 onnxruntime-genai 的 native lib ABI
# 不匹配会导致 frozen exe 加载 genai so 失败。以下版本经板端 frozen 构建实测兼容。
onnxruntime==1.28.0
onnxruntime-genai==0.15.2
onnx==1.22.0
sqlite-vec==0.1.9
tokenizers==0.23.1

# ── QQ Bot ──
qq-botpy==1.2.1

# ── 搜索（可选；web_tools_v2/web_browse_tools 懒加载守卫导入）──
tavily-python==0.7.26
primp==1.3.1
lxml==6.1.1

# ── 配置热重载 / HTTP 客户端 ──
watchdog==6.0.0
requests==2.34.2

# ── 文档处理 ──
pdfplumber==0.11.9
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
html2text==2025.4.15

# ── 认知架构 ──
networkx==3.6.1

# ── 桌面模式（Windows pywebview 原生窗口；Linux 板端不装）──
pywebview==5.4

# 注: pywinpty 为 win32 专属（requirements.txt 带环境标记），Linux 锁文件不含。

# ── 开发/测试 ──
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-timeout==2.3.1
# CI 全集测试带 --cov 生成覆盖率（ci-tests.yml）；此前 lock 缺失导致 --cov 报错
# exit 4，又被 `| tail -50` 吞成假绿——两者都已修复（2026-08-24 门禁复审）。
pytest-cov==7.0.0
ruff==0.15.21
